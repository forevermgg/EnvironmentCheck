package com.mgg.checkenv.rom

/**
 * @author Dylan Cai
 */

/**
 * Returns whether the rom is made by huawei. This is equivalent to calling:
 * ```
 * RomUtils.isHuawei()
 * ```
 */
inline val isHuaweiRom get() = RomUtils.isHuawei()

/**
 * Returns whether the rom is made by vivo. This is equivalent to calling:
 * ```
 * RomUtils.isVivo()
 * ```
 */
inline val isVivoRom get() = RomUtils.isVivo()

/**
 * Returns whether the rom is made by xiaomi. This is equivalent to calling:
 * ```
 * RomUtils.isXiaomi()
 * ```
 */
inline val isXiaomiRom get() = RomUtils.isXiaomi()

/**
 * Returns whether the rom is made by oppo. This is equivalent to calling:
 * ```
 * RomUtils.isOppo()
 * ```
 */
inline val isOppoRom get() = RomUtils.isOppo()

/**
 * Returns whether the rom is made by leeco. This is equivalent to calling:
 * ```
 * RomUtils.isLeeco()
 * ```
 */
inline val isLeecoRom get() = RomUtils.isLeeco()

/**
 * Returns whether the rom is made by 360. This is equivalent to calling:
 * ```
 * RomUtils.is360()
 * ```
 */
inline val is360Rom get() = RomUtils.is360()

/**
 * Returns whether the rom is made by zte. This is equivalent to calling:
 * ```
 * RomUtils.isZte()
 * ```
 */
inline val isZteRom get() = RomUtils.isZte()

/**
 * Returns whether the rom is made by oneplus. This is equivalent to calling:
 * ```
 * RomUtils.isOneplus()
 * ```
 */
inline val isOneplusRom get() = RomUtils.isOneplus()

/**
 * Returns whether the rom is made by nubia. This is equivalent to calling:
 * ```
 * RomUtils.isNubia()
 * ```
 */
inline val isNubiaRom get() = RomUtils.isNubia()

/**
 * Returns whether the rom is made by coolpad. This is equivalent to calling:
 * ```
 * RomUtils.isCoolpad()
 * ```
 */
inline val isCoolpadRom get() = RomUtils.isCoolpad()

/**
 * Returns whether the rom is made by lg. This is equivalent to calling:
 * ```
 * RomUtils.isLg()
 * ```
 */
inline val isLgRom get() = RomUtils.isLg()

/**
 * Returns whether the rom is made by google. This is equivalent to calling:
 * ```
 * RomUtils.isGoogle()
 * ```
 */
inline val isGoogleRom get() = RomUtils.isGoogle()

/**
 * Returns whether the rom is made by samsung. This is equivalent to calling:
 * ```
 * RomUtils.isSamsung()
 * ```
 */
inline val isSamsungRom get() = RomUtils.isSamsung()

/**
 * Returns whether the rom is made by meizu. This is equivalent to calling:
 * ```
 * RomUtils.isMeizu()
 * ```
 */
inline val isMeizuRom get() = RomUtils.isMeizu()

/**
 * Returns whether the rom is made by lenovo. This is equivalent to calling:
 * ```
 * RomUtils.isLenovo()
 * ```
 */
inline val isLenovoRom get() = RomUtils.isLenovo()

/**
 * Returns whether the rom is made by smartisan. This is equivalent to calling:
 * ```
 * RomUtils.isSmatrisan()
 * ```
 */
inline val isSmartisanRom get() = RomUtils.isSmartisan()

/**
 * Returns whether the rom is made by htc. This is equivalent to calling:
 * ```
 * RomUtils.isHtc()
 * ```
 */
inline val isHtcRom get() = RomUtils.isHtc()

/**
 * Returns whether the rom is made by sony. This is equivalent to calling:
 * ```
 * RomUtils.isSony()
 * ```
 */
inline val isSonyRom get() = RomUtils.isSony()

/**
 * Returns whether the rom is made by gionnee. This is equivalent to calling:
 * ```
 * RomUtils.isGionee()
 * ```
 */
inline val isGioneeRom get() = RomUtils.isGionee()

/**
 * Returns whether the rom is made by motorola. This is equivalent to calling:
 * ```
 * RomUtils.isMotorola()
 * ```
 */
inline val isMotorolaRom get() = RomUtils.isMotorola()

/**
 * Returns the [RomUtils.RomInfo]. This is equivalent to calling:
 * ```
 * RomUtils.getRomInfo()
 * ```
 */
inline val romInfo: RomUtils.RomInfo get() = RomUtils.getRomInfo()
